using System.Globalization;
using System.Threading.Tasks;
using Xunit;

namespace DesafioTecnico.Calculos.Test.API
{
    public class CalculosJurosTests
    {
        public CalculosJurosTests()
        {
                
        }

        //public async Task Obter_CalcularJuros_RetornarSucesso(string valorInicial, string meses, string resultadoEsperado)
        //{
        //    var path = $"/calculajuros?valorinicial={valorInicial}&meses={meses}";
        //    var result = await _client.GetAsync(path);
        //    result.EnsureSuccessStatusCode();
        //    var resultString = await result.Content.ReadAsStringAsync();
        //    resultString.Should().Be(resultadoEsperado);
        //}
    }
}
